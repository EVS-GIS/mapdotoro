# Prepare elevation profiles to database export.

Prepare elevation profiles to database export.

## Usage

``` r
prepare_elevation_profiles(dataset = input_elevation_profiles)
```

## Arguments

- dataset:

  data.frame elevation profiles imported.

## Value

data.frame elevation profiles prepared.
